import React from 'react';
// import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet';
import './Eventdetail.scss';

export default function EventDetails() {
  return (
    <>
      <div>
        <h1 className="main-title">Réserver un évènement</h1>
      </div>
    </>
  );
}
