import React from 'react';
import ScrollUpButton from 'react-scroll-up-button';
import './ScrollButton.scss';

const ScrollButton = () => {
  return (
    <div>
      <ScrollUpButton ContainerClassName="scrollButton" />
    </div>
  );
};

export default ScrollButton;
